import { RewriteAsObjectByTypePipe } from './rewrite-as-object-by-type.pipe';

describe('RewriteAsObjectByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RewriteAsObjectByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
